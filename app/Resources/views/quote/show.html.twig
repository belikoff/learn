{% extends 'base.html.twig' %}

{% block body %}
    <h1>Quote</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quote.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ quote.text }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('authorquote_quote_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('authorquote_quote_edit', { 'id': quote.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
